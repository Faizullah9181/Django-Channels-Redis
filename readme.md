**Django With Django Channels and Redis with Celery
Docker Compose**


* docker-compose run django
* docker-compose up
* docker exec -it django sh
* docker-compose run django python manage.py migrate

